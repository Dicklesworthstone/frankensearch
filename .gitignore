# Rust / Cargo
/target/
**/target/
**/*.rs.bk
Cargo.lock

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# bv (beads viewer) local config and caches
.bv/

# ONNX models and ML artifacts (large, downloaded at runtime)
*.onnx
*.safetensors
models/

# Coverage and profiling
*.profraw
*.profdata
lcov.info
tarpaulin-report.html

# Benchmarks (generated)
flamegraph.svg
perf.data
perf.data.old

# Environment
.env
.env.*

# Local scratch DBs
storage.sqlite3
storage.sqlite3-*

# Compiled binaries
a.out

# FastEmbed model caches (downloaded at runtime)
.fastembed_cache/

# Codex build artifacts
target_codex/
target_*/
target-codex*/

# Remote compilation helper target pool. Named plural with no separator; a
# symlink into /data/tmp/rch-targets-pool, regenerated per job.
.rch-targets/
.rch-target*/

# Agent scratch trees. Grow without bound and are reclaimed by mtime.
.scratch/

# Compiled Rust artifacts left behind by ad-hoc lint/probe builds run outside
# any target directory (e.g. liblint_probe.rlib at the repo root).
*.rlib

# Compiler crash reports
rustc-ice-*.txt

# Destructive Command Guard local state
.dcg/

# Agent coordination config
.agent-mail.yaml

# Beads history snapshots
.beads/.br_history/

# Ephemeral data directories (test/benchmark artifacts)
data/e2e_optimize_runs/
data/pressure_artifacts/
data/tmp_manifest/

# Accidental query files
search.submit_query

# bv graph output
--graph-format.svg

# Ephemeral files (agent-generated, build artifacts, test detritus)
ci-artifacts/
proptest-regressions/
test_meminfo
test_logs/

# Claude Code IDE config
.claude/

# Runtime index/vector data (generated by frankensearch-fsfs)
**/.frankensearch/

# NTM session state
.ntm/

# Quill conformance-gauntlet run artifacts
.gauntlet/

# Root-level agent-generated scripts and scratch output
*.log
/*.txt
/*.py
/*.sh
/*.err
/*.json
!package.json
/*.diff
nohup.out

# Core dumps and agent config backups
core.*
core
*.mcp.json.*.bak
cline.mcp.json
codex.mcp.json
cursor.mcp.json
gemini.mcp.json
opencode.json
windsurf.mcp.json
.opencode.json.*.bak
.agent-mail-project-id

# Agent session ephemeral files
SESSION_TODO.md
SESSION_REPORT.md
SESSION_*.md
verify_*.sh
newfile.txt
repro_*.rs

# Claude local settings
.claude/settings.local.json

# Beads ephemeral artifacts
.beads/*.bad_*
.beads/*.backup_*
.beads/*.rebuild_*
.beads/recovery*/
.beads_recovery/

# Beads recovery scratch (machine-local, never tracked)
.beads/.br_recovery/
